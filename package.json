{
  "name": "tabula",
  "version": "1.0.0",
  "description": "One more app for taking your daily notes and organizing work right in your browser.",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run lint && npm run test && npm run clean && mkdir dist && parcel build src/tabula.html --no-source-maps --dist-dir dist",
    "test": "jest",
    "lint": "eslint ."
  },
  "keywords": [
    "notes",
    "browser",
    "notes",
    "work",
    "management",
    "productivity"
  ],
  "author": "Dmytro Nedzelskyi",
  "license": "ISC",
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "jsdom"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.2.5",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.45.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "parcel": "^2.9.1",
    "rimraf": "^5.0.1",
    "ts-jest": "^29.1.2"
  }
}
